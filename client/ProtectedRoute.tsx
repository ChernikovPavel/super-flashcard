import { Navigate } from "react-router-dom";
// import { ProtectRouteProps } from "./types/propTypes";

// export default function ProtectedRoute({ children, authUser, redirectTo }: {
//   children: JSX.Element,
//   authUser: string | undefined,
//   redirectTo: string
// }): JSX.Element {
//   if (authUser) {
//     return <Navigate to={redirectTo} replace/>
//   } else {
//     return children
//   }
// }

// export default function ProtectedRoute({ children, authUser, redirectTo }: ProtectRouteProps): JSX.Element {
//   if (authUser) {
//     return <Navigate to={redirectTo} replace/>
//   } else {
//     return children
//   }
// }
